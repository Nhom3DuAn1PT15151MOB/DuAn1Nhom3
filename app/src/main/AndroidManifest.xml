<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.baitest1">
    <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_backgroud_action"
        android:label="Quản lý thu chi"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".sua.SuaKhoanChiActivity"></activity>
        <activity android:name=".sua.SuaLoaiChiActivity" />
        <activity android:name=".them.ThemKhoanThuActivity" />
        <activity android:name=".them.ThemLoaiThuActyvity" />
        <activity android:name=".them.ThemKhoanChiActivity" />
        <activity android:name=".them.ThemLoaiChiActivity" />
        <activity android:name=".DiaLogAdd" />
        <activity
            android:name=".MainActivity"
            android:label="Quản lý thu chi"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>